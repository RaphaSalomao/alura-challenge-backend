name: Deploy master
on:
  push:
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout source code
      uses: actions/checkout@v2

    - name: Generate deployment package
      run: zip -r deploy.zip . -x '*.git*'

    - name: Deploy to EB
      uses: einaregilsson/beanstalk-deploy@v20
      with:
        aws_access_key: AKIATWRRMI6I55ZXA3ZU
        aws_secret_key: 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
        application_name: alura-challenge-backend-2nd-edition
        environment_name:  Alurachallengebackend2ndedition-env
        version_label: alura-challenge-backend-2nd-edition-source-2
        region: us-east-2
        deployment_package: deploy.zip
